**Pubdater.io** is a lightweight web app that helps Dart and Flutter developers automatically update their `pubspec.yaml` dependencies to align with a target Dart SDK version.  
It provides a simple interface to paste your `pubspec.yaml`, choose the desired SDK version, and get updated dependency contents instantly — with progress tracking and console feedback.

---

## ✨ Features
- 📄 Input your `pubspec.yaml` contents directly in the app.
- 🔢 Specify the target Dart SDK version.
- ⚡ One‑click processing with a **Submit** button.
- 📊 Real‑time progress bar with percentage updates.
- 🖥️ Console output showing status, errors, and completion messages.
- 📝 Updated dependencies displayed in a separate output area.

